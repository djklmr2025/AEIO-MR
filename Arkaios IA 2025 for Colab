{
  "cells": [
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {
        "id": "VjYy0F2gZIPR"
      },
      "outputs": [],
      "source": [
        "# Reiniciar completamente el entorno\n",
        "import os\n",
        "import shutil\n",
        "\n",
        "# Asegurarse de estar en el directorio raíz\n",
        "os.chdir('/content')\n",
        "\n",
        "# Eliminar completamente el directorio\n",
        "if os.path.exists('AEIO-MR'):\n",
        "    shutil.rmtree('AEIO-MR')\n",
        "\n",
        "# Clonar fresh\n",
        "!git clone https://github.com/djklmr2025/AEIO-MR\n",
        "\n",
        "# Entrar al directorio\n",
        "%cd /content/AEIO-MR\n",
        "\n",
        "# Instalar dependencias básicas primero\n",
        "!pip install flask flask-cors flask-socketio eventlet\n",
        "!pip install pyngrok\n",
        "\n",
        "# --- CONFIGURAR NGROK CON TU TOKEN ---\n",
        "from pyngrok import ngrok\n",
        "\n",
        "# TU TOKEN DE NGROK\n",
        "NGROK_AUTH_TOKEN = \"32AwHmDcHcu8VoBjfh67UQ6EGFo_4fUMiZbTuPkXr4fnLYFhC\"\n",
        "ngrok.set_auth_token(NGROK_AUTH_TOKEN)\n",
        "print(\"✅ Token de ngrok configurado correctamente\")\n",
        "\n",
        "# --- EJECUTAR SERVIDOR EN SEGUNDO PLANO ---\n",
        "import subprocess\n",
        "import time\n",
        "\n",
        "# Iniciar el servidor en background (usando nohup para que no se detenga)\n",
        "print(\"🚀 Iniciando servidor ARKAIOS en segundo plano...\")\n",
        "server_process = subprocess.Popen([\n",
        "    'nohup', 'python', 'server_arkaios.py', '--share', '--always-high-vram', '&'\n",
        "])\n",
        "\n",
        "# Esperar a que el servidor inicie completamente\n",
        "print(\"⏳ Esperando 15 segundos para que el servidor inicie...\")\n",
        "time.sleep(15)\n",
        "\n",
        "# Verificar que el servidor está corriendo\n",
        "!ps aux | grep python | grep server_arkaios\n",
        "\n",
        "# --- CREAR TÚNEL NGROK ---\n",
        "print(\"🌐 Creando túnel ngrok...\")\n",
        "public_url = ngrok.connect(5000)\n",
        "print(\"🎉 ¡TU SERVIDOR ESTÁ ACTIVO!\")\n",
        "print(\"🔗 URL Pública:\", public_url)\n",
        "print(\"📱 Comparte este enlace: \" + public_url)\n",
        "print(\"🏠 Local: http://127.0.0.1:5000\")\n",
        "print(\"📚 Docs: \" + public_url + \"/docs\")\n",
        "\n",
        "# Mostrar logs del servidor para verificar\n",
        "print(\"\\n📋 Últimas líneas del log del servidor:\")\n",
        "!tail -10 nohup.out 2>/dev/null || echo \"No hay logs aún, esperando...\"\n",
        "\n",
        "# --- MANTENER ACTIVO Y MONITOREAR ---\n",
        "import requests\n",
        "import threading\n",
        "\n",
        "def check_server_status():\n",
        "    \"\"\"Función para verificar cada minuto que el servidor sigue activo\"\"\"\n",
        "    while True:\n",
        "        try:\n",
        "            response = requests.get(\"http://127.0.0.1:5000/health\", timeout=10)\n",
        "            print(f\"✅ Servidor saludable ({response.status_code}) - {time.strftime('%H:%M:%S')}\")\n",
        "        except Exception as e:\n",
        "            print(f\"❌ Error conectando al servidor: {e}\")\n",
        "            print(\"🔄 Intentando reiniciar servidor...\")\n",
        "            os.system(\"pkill -f server_arkaios.py\")\n",
        "            subprocess.Popen(['nohup', 'python', 'server_arkaios.py', '--share', '--always-high-vram', '&'])\n",
        "            time.sleep(10)\n",
        "\n",
        "        time.sleep(60)\n",
        "\n",
        "# Iniciar monitoreo en segundo plano\n",
        "monitor_thread = threading.Thread(target=check_server_status, daemon=True)\n",
        "monitor_thread.start()\n",
        "\n",
        "print(\"\\n\" + \"=\"*60)\n",
        "print(\"🚀 SERVIDOR ARKAIOS EJECUTÁNDOSE EN SEGUNDO PLANO\")\n",
        "print(\"🌐 URL PÚBLICA:\", public_url)\n",
        "print(\"📊 Monitoreo activo - El servidor se reiniciará automáticamente si falla\")\n",
        "print(\"=\"*60)\n",
        "\n",
        "# Mantener el proceso principal activo\n",
        "try:\n",
        "    while True:\n",
        "        time.sleep(3600)  # Esperar 1 hora entre checks principales\n",
        "        print(\"⏰ Verificación periódica - Todo OK\")\n",
        "except KeyboardInterrupt:\n",
        "    print(\"🛑 Deteniendo servidor y ngrok...\")\n",
        "    os.system(\"pkill -f server_arkaios.py\")\n",
        "    os.system(\"pkill -f ngrok\")\n",
        "    print(\"✅ Procesos detenidos\")"
      ]
    }
  ],
  "metadata": {
    "accelerator": "GPU",
    "colab": {
      "gpuType": "T4",
      "provenance": []
    },
    "kernelspec": {
      "display_name": "Python 3",
      "name": "python3"
    },
    "language_info": {
      "name": "python"
    }
  },
  "nbformat": 4,
  "nbformat_minor": 0
}